# ============================================
# Coolify Deploy - V2 (guelaguetzav2.mdconsultoria-ti.org)
# ============================================
# Variables de entorno requeridas en Coolify:
#   POSTGRES_PASSWORD - Password de PostgreSQL
#   JWT_SECRET       - Secret para JWT tokens
# ============================================

services:
  postgres:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_DB: guelaguetza_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-guelaguetza_secure_2026}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d guelaguetza_db"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 60s

  redis:
    image: redis:7-alpine
    restart: always
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    volumes:
      - backend-uploads:/app/public
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:-guelaguetza_secure_2026}@postgres:5432/guelaguetza_db
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:-guelaguetza_jwt_secret_2026}
      COOKIE_SECRET: ${COOKIE_SECRET:-${JWT_SECRET:-guelaguetza_jwt_secret_2026}}
      CORS_ORIGINS: ${CORS_ORIGINS:-https://guelaguetzav2.mdconsultoria-ti.org,http://guelaguetzav2.mdconsultoria-ti.org}
      AUTO_SEED: "true"
      ENABLE_PAYMENTS: "false"
      ENABLE_NOTIFICATIONS: "false"
      ENABLE_ANALYTICS: "false"
      LOG_LEVEL: info
    user: "root"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        VITE_API_URL: "https://guelaguetzav2.mdconsultoria-ti.org"
    restart: always
    ports:
      - "3082:80"
    depends_on:
      backend:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  postgres-data:
  redis-data:
  backend-uploads:
